#!/usr/bin/env python2
# -*- coding: utf-8 -*-

def calc_cash_inventory():
    
    if c.par.country != a.par.country:
        output[c] = c.previous_var * (1+c.par.nominal_interest_rate) + maturing_assets

    
    if c.par.country == a.par.country:
    
        output[c] = c.previous_var * (1+c.par.nominal_interest_rate) + maturing_assets
        
        return output 